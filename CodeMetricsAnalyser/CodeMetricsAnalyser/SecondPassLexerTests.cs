using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using CodeMetricsAnalyser;
using System.IO;
using System.Collections.Generic;

namespace TestsForCodeMetricsAnalyser
{
    class SecondPassLexerTests
    {

        public void Lex_simple_hash_include_token()
        {/*
           stringstream ss;
           ss << "#include";

           Token expected = new Token(0, 0);
           expected.Text = "#include";
           List<Token> input;
           input.Add(expected);

           token_list output = secondPassLexer.SplitTokens(input);
   
           ASSERT_TOKENLISTS_IDENTICAL(input, output);*/
        }
    }
}
